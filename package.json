{
  "name": "registration-vue",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "main": "server/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-and-serve": "npm run build-client && npm run start-server",
    "build-client": "npm install && npm run build",
    "start-server": "npm install && cd server && node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "sass": "^1.77.6",
    "vue": "^3.4.29"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "vite": "^5.3.1"
  }
}
